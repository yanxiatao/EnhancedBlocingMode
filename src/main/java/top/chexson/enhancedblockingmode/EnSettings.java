package top.chexson.enhancedblockingmode;

import appeng.api.config.Setting;

public class EnSettings {
    public static Setting<EnhanceBlockingMode> ENHANCED_BLOCKING_MODE = new Setting<>("enhanced_blocking_mode", EnhanceBlockingMode.class);
}
